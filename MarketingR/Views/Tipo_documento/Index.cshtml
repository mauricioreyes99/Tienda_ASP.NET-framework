@model IEnumerable<MarketingR.Models.Tipo_documento>

@{
    ViewBag.Title = "Index";
}

<h2>Documentos</h2>
<div class="container">
    <div class="col-sm-10"></div>
    @Html.ActionLink("Create New", "Create", new { }, new { @class = "btn btn-primary" })
</div>
<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_tipoDocumento }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_tipoDocumento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_tipoDocumento })
        </td>
    </tr>
}

</table>
